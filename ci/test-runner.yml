---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: python:3.8

inputs:
- name: src-repository

run:
  path: sh
  args:
  - -c
  - |
    cd src-repository/
    pip install --upgrade pip
    ./dev install-all-reqs
    ./dev tests
